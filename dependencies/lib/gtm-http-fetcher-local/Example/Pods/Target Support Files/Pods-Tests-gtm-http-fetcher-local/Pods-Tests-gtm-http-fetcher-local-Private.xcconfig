#include "Pods-Tests-gtm-http-fetcher-local.xcconfig"
GCC_PREPROCESSOR_DEFINITIONS = $(inherited) COCOAPODS=1
HEADER_SEARCH_PATHS = "${PODS_ROOT}/Headers/Build" "${PODS_ROOT}/Headers/Build/gtm-http-fetcher-local" "${PODS_ROOT}/Headers/Public" "${PODS_ROOT}/Headers/Public/gtm-http-fetcher-local"
OTHER_LDFLAGS = -ObjC
PODS_ROOT = ${SRCROOT}